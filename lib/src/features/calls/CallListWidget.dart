import 'package:flutter/material.dart';

class CallListWidget extends StatelessWidget {
  const CallListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Call List Widget');
  }
}
