import 'package:flutter/material.dart';

class CommunityListWidget extends StatelessWidget {
  const CommunityListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Community List Widget');
  }
}
